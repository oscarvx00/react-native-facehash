---
"react-native-facehash": minor
---

Add `FacehashProvider` context to set shared default props (`colors`, `size`, `variant`, `intensity3d`, `shape`, `showInitial`, `faceColor`) for all `Facehash` instances in a subtree. Individual instances can still override any value.
